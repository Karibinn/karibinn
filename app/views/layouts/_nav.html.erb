<nav class="row navbar navbar-expand-lg navbar-light bg-light justify-content-between">
  <%= link_to 'Karibinn', root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="#">Activities</a>
      </li>
      <li class="nav-item">
        <%= link_to t('nav.destinations'), properties_path, class: 'nav-link' %>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Get in Touch</a>
      </li>
    </ul>
      <% if signed_in? %>
      <span><%= current_user.email %></span>
      <%= link_to t('layout.logout'), destroy_user_session_path, method: :delete, class: 'nav-link' %>

      <% else %>
      <%= link_to t('layout.login'), new_user_session_path, class: 'nav-link' %>
      <% end %>
  </div>
</nav>
