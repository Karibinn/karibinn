<nav class="navbar navbar-expand-lg navbar-light justify-content-between bg-transparent">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav navbar-right ml-auto">
      <li class="nav-item">
        <a class="nav-link" href="#">Activities</a>
      </li>
      <li class="nav-item">
        <%= link_to t('nav.destinations'), properties_path, class: ['nav-link', current_page?(properties_path) ? 'active' : nil] %>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Your Journey</a>
      </li>
      <% if signed_in? %>
      <li class="nav-item">
          <span class="nav-link"><%= current_user.email %></span>
      </li>
      <% end %>
      <li class="nav-item">
        <% if signed_in? %>
          <%= link_to t('layout.logout'), destroy_user_session_path, method: :delete, class: 'nav-link' %>
        <% else %>
          <%= link_to t('layout.login'), new_user_session_path, class: 'nav-link' %>
        <% end %>
      </li>
    </ul>
  </div>
</nav>
