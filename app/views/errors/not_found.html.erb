<div class="container">
  <div class="row">
    <div class="col-md-10">
      <% @search_form = PropertySearchForm.new %>
      <%= render 'shared/search_pills', url: properties_path %>
    </div>
  </div>

  <hr class="mb-5"/>

  <div class="row">
    <div class="col-md-10 text-center">
      <%= image_tag(image_path('404.svg'), class: 'img-fluid w-75') %>

      <h2 class="my-3 text-center display-4">
        Oups! Aucune destination de voyage extraordinaire trouvée ici!
      </h2>
    </div>
  </div>

</div>

<div class="bg-dark full-width py-5 mt-5">
  <%= render 'shared/locations', locations: Location.all, header: t('pages.404.locations_header') %>
</div>
